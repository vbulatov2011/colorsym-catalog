# transient render jobs (built by tools/make_entry_job.py, run, then deleted)
jobs/delta_*.json

# editors / OS
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/

# node
node_modules/
npm-debug.log*

# rendered PNGs are intermediates: the job writes them, tools/to_webp.py
# converts each entry to lossless WebP (identical pixels, 12% of the size)
**/gen/*.png
